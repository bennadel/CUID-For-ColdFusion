component
	output = false
	hint = "I provide the application settings and event handlers for testing." 
	{

	this.mappings[ "/lib" ] = ( getDirectoryFromPath( getCurrentTemplatePath() ) & "../lib/" );

}
